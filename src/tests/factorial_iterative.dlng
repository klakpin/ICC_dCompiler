var fact := func(n)
        if n < 0 then
            print "No negative numbers"
            return 0
        end

        var ans := 1
        for i in 1 .. n loop
            ans := ans * i
        end

        return ans
    end
